%h1 Friends activities

- @activities.each do |activity|
  .activity
    = link_to activity.owner.username, activity.owner if activity.owner
    added comment to
    = link_to activity.trackable.title, activity.trackable